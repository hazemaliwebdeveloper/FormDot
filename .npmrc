# NPM Configuration
# This ensures consistent behavior across environments

# Use exact versions (no ~ or ^)
save-exact=false

# Set production environment for Vercel
# production=false

# Disable optional dependencies that might not be needed
# optional=false